SERVER
	socket:	create a socket, which is an endpoint for comunication
	bind:	bind the socket, essentially initialize it
	listen: tell the world that you are listening at this socket (non blocking, tells the socket tht you are a server socket)

CLIENT
	socket:
	connect:
		SERVER
			accept: server accepts connect form the client (blocking)
	send:
			recieve
			send
	recieve:

